import React, { useState } from 'react'

export default function Pagination({count = 0, current = 1, onChange, maxToShow = 6, arrows = false}) {
    
    // function getNumbers(current) {
    //     let numbers = [];
    //     if (count && count > 0) {
    //         if (count <= maxToShow) {
                
    //         }
    //     }
    // }

    return (
        <div className="pagination">
            
        </div>
    )
}
